let ctx = window.location.pathname.substring(0, window.location.pathname.indexOf("/",2));
let status = true;

function company(ths){
	var hello = $(ths).text();
	alert(hello);
}
function genre(ths){
	var hello = $(ths).text();
	alert(hello);
}
function country(ths){
	var hello = $(ths).text();
	alert(hello);
}